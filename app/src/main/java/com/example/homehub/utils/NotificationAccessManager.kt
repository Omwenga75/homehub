package com.example.homehub.utils

object NotificationAccessManager {
    fun initialize() {
        // Initialization logic for notification access if needed
    }

    fun hasAccess(): Boolean = true
}
